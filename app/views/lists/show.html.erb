<!--
<div class="container">
  <div class="card-show-title">
    <h1>Movies 🎬</h1>
    <p>
      <strong>Name:</strong>
      <%= @list.name %>
      <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Overview</th>
          <th>Poster url</th>
          <th>Rating</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @list.movies.each do |movie| %>
          <tr>
            <td><%= movie.title %></td>
            <td><%= movie.overview %></td>
            <td><%= movie.poster_url %></td>
            <td><%= movie.rating %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    </p>
  </div>
        -->

  <!--
    <div class="row cards-movie">
      <% @list.bookmarks.each do |bookmark| %>
        <div class="col-6-sm">
          <div class="card-movie">
          <div class="card-poster">
              <%= image_tag bookmark.movie.poster_url, alt: "movie poster" %>
          </div>
          <div class="card-description">
            <div class="card-title">
              <h5><%= bookmark.movie.title %></h5>
              <p> <%= bookmark.movie.rating %> ⭐️</p>
            </div>
            <div class="card-overview">
              <p> <%= bookmark.movie.overview %> </p>
            </div>
            <hr>
``           <p><%= bookmark.comment %></p>
            <button class="delete-movie btn btn-danger">Delete</button>
            <%= link_to "", bookmark_path(bookmark), method: :delete, id:"delete-bookmark", class: "hidden" %>
            </div>
          </div>
        </div>
      </div>
      <% end %>
  </div>
        -->

<div class="container">
  <div class="movie-content">
    <div class="row">
      <div class="col-lg-6">
        <h2><%= @list.name %> Movies</h2>
        <div class="btn-style">
          <p><%= link_to 'Add Movie to List (Create a bookmark)',  new_list_bookmark_path(@list), class: 'btn btn-outline-primary' %></p>
        </div>
       </div>
    </div>

    <hr>

  <div class="row row-cols-1 row-cols-md-2 g-4">
    <% @list.movies.each do |movie| %>
      <div class="col">
        <div class="card h-100">
          <img src="<%= movie.poster_url %>" class="card-img-top" alt="<%= movie.title %>">
          <div class="card-body">
            <h5 class="card-title"><%= movie.title %></h5>
            <p class="card-text"><%= movie.overview %></p>
            <% movie.bookmarks.each do |bookmark| %>
              <strong>Comment: </strong>
              <p class="card-text"><%= bookmark.comment if bookmark.list_id == @list.id %></p>
            <% end %>
          </div>
            <div class="card-footer">
              <p class="card-text"><%= movie.rating %></p>
            </div>
        </div>
      </div>
    <% end %>
  </div>

</div>

<%= link_to 'Edit', edit_list_path(@list) %>
<%= link_to 'Back', lists_path %>
